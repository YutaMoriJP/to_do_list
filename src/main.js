import React from 'react';
import ReactDOM from 'react-dom';
import DisplayNote from '../component/displayNote';
import { DisplayNotes } from '../component/noteFunc';

ReactDOM.render(<DisplayNote />, document.getElementById('root'));
